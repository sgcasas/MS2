DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# URL pública por la que se accede a esta API (usada en Swagger/clientes,
# mapea a app.public-api-url en application.yml). En producción: la IP
# pública o el dominio de la VM de app, con puerto, p. ej.
# http://<ip-publica-o-dominio>:8082
PUBLIC_API_URL=
